# SPEC.md — AI Workspace 設計仕様書（B改訂版・確定）

> **このドキュメントの位置づけ**
> これは Claude Code への引き渡し仕様書です。複数回の設計レビューを経て確定した内容を1ファイルに蒸留しています。
> Claude Code はこの仕様に従って、まず §11「フェーズ1で作るもの」を実装してください。
> §12「まだ作らないもの」に挙げた要素は、明示的に指示があるまで作成しないこと。
> 仕様と異なる判断が必要になった場合は、勝手に進めず人間に確認すること。

---

## 1. 目的と制約

このシステムの目的は「24時間自律AI組織」を作ること**ではない**。それは手段にすぎない。

真の目的は、以下の4制約を同時に満たしながら知的労働の成果量を増やすこと。

1. **品質**を落とさない
2. **コスト**を抑える
3. **人間の関与時間を1日20〜30分以内**に収める
4. **信頼性・監査性**を保つ

制約3（人間の関与時間）は第一級の設計変数として扱う。これを無視した自作AIシステムは3ヶ月で形骸化する。設計判断に迷ったら、常に「人間の手間が増えないか」を基準にする。

---

## 2. 設計原則（7原則）

1. **エージェントは人格ではなく「文脈・権限の隔離装置」**。分ける理由はコンテキスト衛生とツール/権限の限定だけ。だから数は最小にする。
2. **専門性は人数ではなくスキルに持たせる**。固定人格を増やさず、汎用シェル＋着脱式スキルにする。
3. **正本は1つ**。Git管理リポジトリ。実行中の可変状態だけは別ストアに分離する（§3.6）。
4. **人間が触る面は1つ**。朝のブリーフ＋承認キュー。
5. **信頼勾配を背骨にする**。自動化レベル・権限を1軸（T0〜T2）に畳む。
6. **検査は既定1回、リスク時だけ増やす**。固定多段レビューはしない。
7. **証拠で育てる**。最小から始め、実測した失敗が拡張を正当化したときだけ足す。

---

## 3. アーキテクチャ

### 3.1 実行主体：3つの基本定義＋2つの条件付き機能

「固定4エージェント」ではなく **「3基本＋2条件付き」** が正確な表現。

| 区分 | 名前 | 起動タイミング |
|------|------|----------------|
| 基本 | Knowledge Doer | 調査・分析・文書タスク時 |
| 基本 | Build Doer | コード・ファイル変更タスク時 |
| 基本 | Checker | 全タスクの検査工程（T0は任意） |
| 条件付き | Planner | 依頼受付時の下書き補助（人間が確認） |
| 条件付き | Auditor | T2タスクのみ |

指揮役（オーケストレーター）は初期は**人間自身**が担う。AIによるタスク分解の自動化はフェーズ3要素。

### 3.2 Doerは「職種」ではなく「権限境界」で分ける

分割の根拠は業務領域ではなく、ツール権限の境界（＝セキュリティ境界）。

**Knowledge Doer（読み取り中心）**
- 許可：ファイル閲覧、検索、分析、Markdown/下書き作成、指定成果物フォルダへの出力
- 禁止：Bash実行、アプリコード変更、パッケージ追加、外部送信、削除
- 用途：調査、要件整理、UX分析、文書作成、比較、仕様案

**Build Doer（書き込み・実行）**
- 許可：開発対象ファイルの読み書き、Bash実行、テスト、ビルド、開発ブランチへのコミット
- 禁止：本番ブランチへの直接反映、外部送信、本番データ変更、認証情報の変更、人間承認なしの削除
- 用途：コード実装、テスト作成、バグ修正、開発環境整備

Operations Doer は初期に作らない。定期処理も内容に応じて Knowledge か Build を呼ぶ。

### 3.3 専門性はスキルで追加する

専門知識はスキルとしてパッケージ化し、必要なときにシェルへ読み込ませる。権限・文脈の隔離が必要になった場合のみサブエージェント化する。

初期スキル（7個）：

```
requirements-analysis     要件整理
task-decomposition        タスク分解
source-verification       出典・根拠検証 ← 「根拠なし記述率」指標の背骨。必須。
ui-ux-review              UI/UXレビュー
code-implementation       実装
qa-testing                テスト
security-check            セキュリティ検査
```

財務分析・ブランド文体・競合調査などは、実際に必要になってから追加する。

### 3.4 リスクティア（信頼勾配）

各タスクにティアを1つだけ付け、それが人間の関与量を決める。

| ティア | 例 | 検査フロー |
|--------|-----|-----------|
| **T0** 下書き | アイデア、要約、UX改善案、調査メモ、比較案 | Doer → scratch出力 →（任意で人間が読む）。**Checker任意** |
| **T1** 隔離変更 | Markdown更新、開発ブランチ、テストコード、リファクタ、タスク整理 | Doer → **Checker必須** → 人間がマージ |
| **T2** 不可逆・外部 | 外部送信、本番反映、削除、顧客データ、権限変更、課金、セキュリティ設定 | Doer → Checker → **Auditor** → **人間承認** → 実行 |

**T2はクラウドRoutinesで自律実行させない**（Routinesは実行時の承認プロンプトがないため）。

各ティアで通す**思考ゲートの濃度**（スコープ／証拠／対立的推論／検証／校正）はティア連動とする。詳細な対応表は `CLAUDE.md §3.1` と `06_Skills/task-decomposition`（ADR-0006 提案1）を正とする。要点：T0＝スコープ＋証拠のみ、T1＝＋対立的推論（着手前3点）＋Checker、T2＝＋校正1回＋Auditor。新スキル・新人格は増やさない。

「ティア判断に迷ったら安全側に倒す」の「迷う」には**依頼者・承認者の専門知識と領域リスクのギャップ（畑違い）**も含める。また、確認・承認文言は抽象的なOK確認ではなく**具体的な現実の結果**を平易に問う（知ったかぶりリスク対策）。詳細は `CLAUDE.md §3` および `§3.2` を正とする。

なりすまし・金銭アクセス・認証突破につながる情報、または個人情報保護法・不正競争防止法等の特別法で保護される情報は、通常のT2手続きに入る前に法務・情報システム部門等の可否判断を挟む（あくまで次善の安全装置であり、そもそもAIが読める場所に置かないことが第一の防御線）。詳細は `CLAUDE.md §3.3` を正とする。

### 3.5 ステータス（初期6つ）

```
todo      実行可能な未着手タスク
doing     Doerが作業中
checking  Checkerが検査中
approval  人間判断待ち
blocked   情報・権限・依存関係待ち
done      受入条件を満たして完了
```

失敗はステータスにせず、実行結果または異常フラグとして記録する。タスクが増えてトリアージ滞留が出たら `inbox` を追加する。

### 3.6 正本と実行状態の分離

「正本は1つ」原則は守りつつ、**ドメインの異なる2つ**を分離する。これは正本の複製ではない。

| ストア | 持つもの | 段階 |
|--------|----------|------|
| **Git（＝Obsidian Vault）** | 指示書・タスク定義・成果物・承認記録・確定ログ等の**確定記録** | フェーズ1から |
| **SQLite（runtime.db, ローカル限定）** | 実行キュー・実行中ステータス・ロック・コスト集計・通知等の**機械的な一時状態** | フェーズ2から |

**フェーズ1ではSQLiteを作らない。** 実行状態は「タスクMarkdown＋実行ごとのJSON Linesログ」で扱う。進行中の状態をGitへ頻繁に書き戻さないこと（差分の氾濫を防ぐ）。タスクが一段落したときだけMarkdownのステータスを更新する。

```
tasks/TASK-0001.md                          ← タスク定義（確定）
10_Runs/TASK-0001/2026-06-26T090000.json    ← 実行結果（追記）
```

### 3.7 ローカル実行系 と クラウドRoutines系 を物理的に分ける

これは現行 Claude Code の実挙動に基づく必須の分離。

**ローカル実行系**（Claude Code Desktop / CLI）
- ローカルファイル・Obsidian Vault・SQLiteにアクセスできる
- 用途：ローカルファイル整理、Vault処理、タスクキュー、ロック、コスト集計、自作アプリ連携、T2を含む承認付き処理

**クラウドRoutines系**
- 実行ごとにリポジトリを clone する。**ローカルSQLiteは見えない**。実行時の承認プロンプトがない。既定では `claude/*` ブランチにしか push できない。
- 用途は**リポジトリ内で完結する処理に限定**：Checker一括掃引、Markdown整合性検査、リンク切れ検査、タスク一覧集計、朝のブリーフ生成、リポジトリ内テスト、ドキュメント更新候補のPR作成
- **使わない**：本番操作、ローカルObsidian処理、SQLite依存処理、T2タスク

---

## 4. 不変条件（最重要・実装前に必ず固定）

### 4.1 可変状態（status）の書き手は1つだけ

タスクの `status` を書き換えられる主体は **ローカルの人間/Claude Code だけ**。

- **Routinesは追記専用（append-only）**。既存タスクの status を絶対に書き換えない。レポートは `10_Runs/` に新規ファイルとして吐くだけ。
- これにより、ローカル（デフォルトブランチ）とRoutine（`claude/*`ブランチ）が同じ status 行を奪い合うマージ衝突を構造的に防ぐ。
- フェーズ1のRoutineも「読む → レポートを新規ファイルで出す → 人間がそれを見て status を動かす」とする。

### 4.2 CheckerとAuditorの独立性は「素材と問いの別さ」から来る

ファイル名を分けるだけでは独立性は担保されない。**異なる素材を異なる角度で**見ることで担保する。

- **Checker**：成果物 vs 受入条件（適合/不適合/判定不能）
- **Auditor**：ログ・承認記録 vs 工程・権限・規定・コスト上限

両者を同一主体にすると、同種の見落とし（相関故障）が残る。可能ならモデルティアも変える（例：Checker=標準、Auditor=高性能）。

### 4.3 指示が矛盾したときの優先順位（固定）

低い階層が高い階層を上書きできない。

```
1. 法令・セキュリティ規定
2. constitution（共通規定）
3. プロジェクト要件
4. 個別タスク指示
5. エージェント固有マニュアル
6. 過去事例・議事録
7. AI自身の推測
```

---

## 5. ディレクトリ構造（Obsidian Vault ＝ Git リポジトリ）

```
ai-workspace/
├─ 00_Inbox/        未整理の依頼
├─ 01_Projects/     プロジェクト文脈
├─ 02_Tasks/        タスク定義（正本）
├─ 03_Outputs/      成果物
├─ 04_Approvals/    人間承認記録
├─ 05_Agents/       エージェント定義
├─ 06_Skills/       専門スキル
├─ 07_Policies/     共通規定（constitution・優先順位・禁止事項）
├─ 08_Decisions/    意思決定記録（ADR）
├─ 09_Reports/      日次ブリーフ等
├─ 10_Runs/         実行ごとのJSON Linesログ（追記専用）
├─ 99_Archive/      アーカイブ
├─ CLAUDE.md        ← 不変条件・優先順位を常時文脈に載せる（§11で生成）
└─ SPEC.md          このファイル
```

`.obsidian/` は共有したい設定だけ Git 管理し、端末固有設定は `.gitignore` に入れる。

---

## 6. タスクスキーマ

`02_Tasks/TASK-YYYY-NNNN.md` の形式。

```yaml
---
task_id: TASK-2026-0001
title: タスク管理画面の長期タスク表示改善案
objective: 達成したいことを1〜2文で
project: PROJECT-001
status: todo            # todo|doing|checking|approval|blocked|done
tier: T1                # T0|T1|T2
owner: knowledge-doer   # knowledge-doer|build-doer
checker: checker
auditor:                # T2のときだけ auditor を記入
requires_human_approval: false   # T2は必ず true
inputs:
  - 03_Outputs/PROJECT-001/current-screen.png
outputs:
  - 03_Outputs/PROJECT-001/ui-proposal.md
acceptance_criteria:
  - 現状課題が5項目以内で整理されている
  - 改善画面の構成が記載されている
  - 実装優先順位が示されている
prohibited:
  - 本番データを変更しない
  - 外部サービスへアップロードしない
max_cost_usd: 3.00
max_attempts: 2
created_at: 2026-06-26T09:00:00+09:00
deadline: 2026-07-03T17:00:00+09:00
---

# 背景
（人間 or Planner が記入）

# 詳細
（補足・制約・参考資料）

# メモ
（作業中の気づき。status はここで動かさず frontmatter で管理）
```

**status は frontmatter の値が唯一の真実。** Doer は `doing → checking` までしか動かせない。`checking → approval` は Checker（および承認ルール）、`approval → done` は人間のみ。§4.1のとおり、Routine はこの値を一切書き換えない。

---

## 7. エージェント定義仕様

`05_Agents/` 配下に Claude Code サブエージェント形式（YAMLフロントマター＋本文）で作成する。
`model` は性能クラスの別名（`haiku`/`sonnet`/`opus`）で指定し、将来差し替え可能とする。

### 7.1 knowledge-doer.md

```yaml
---
name: knowledge-doer
description: 調査・要件整理・分析・文書作成を行う読み取り中心の実行役。Bash実行・コード変更・外部送信・削除は行わない。調査/分析/仕様案/UX分析タスクで使う。
tools: Read, Grep, Glob, Write
model: sonnet
---
```
本文に記載すること：役割、担当範囲、担当外、入力、出力（番号付き構造：現状課題/設計方針/構成/フロー/例外/未解決/引継ぎ）、完了条件（各受入条件を適合/不適合/判定不能で記載）、禁止事項（推測で確定しない・根拠なく断定しない・要件を無断変更しない・完了条件を自分で緩めない）、エスカレーション（必須入力欠落・指示矛盾・権限外・条件未達なら停止して blocked）。

### 7.2 build-doer.md

```yaml
---
name: build-doer
description: 開発ブランチ内でのコード実装・テスト・ビルドを行う実行役。本番反映・外部送信・認証情報変更・無承認削除は行わない。実装/テスト/バグ修正タスクで使う。
tools: Read, Grep, Glob, Write, Edit, Bash
model: sonnet
---
```
本文に記載すること：knowledge-doer と同じ枠組み。加えて「変更は開発ブランチに限定」「保護ブランチへ直接 push しない」「削除・破壊的操作は人間承認」を明記。

### 7.3 checker.md

```yaml
---
name: checker
description: 成果物を受入条件に照合して独立検証する。各条件を適合/不適合/判定不能で判定し、不合格理由を明示してDoerへ戻す。成果物自体を大きく改変しない。
tools: Read, Grep, Glob
model: sonnet
---
```
本文に記載すること：見るのは「成果物 vs 受入条件」のみ。判定は3値（適合/不適合/判定不能）。修正はせず差し戻す。status は `checking → approval` のみ動かせる。

### 7.4 auditor.md（T2専用・条件付き）

```yaml
---
name: auditor
description: T2タスク専用。ログ・承認記録を工程/権限/規定/コスト上限に照合する監査役。成果物の良否ではなく工程の正しさを見る。通常は起動しない。
tools: Read, Grep, Glob
model: opus
---
```
本文に記載すること：見るのは「ログ・承認記録 vs 工程・権限・禁止事項・コスト上限」（§4.2）。確認項目：指定工程を通ったか、承認を飛ばしていないか、根拠が記録されているか、禁止事項に触れていないか、コスト上限内か。Checker とは別の素材を別の角度で見ること。

### 7.5 planner-prompt.md（補助機能・サブエージェントでなくプロンプトテンプレート）

依頼文から §6 のタスクファイル下書きを生成するためのテンプレート。出力：目的/タスク分解/必要スキル/リスクティア/受入条件/人間承認箇所。**生成した計画は実行前に人間が短時間で確認する**（自動実行しない）。

---

## 8. 「静かに壊れる」監視指標（エラーの出ない失敗を捕捉）

フェーズ2以降で計測する。閾値は初期値であり、運用で較正する。

| 指標 | 異常の目安 |
|------|-----------|
| タスク滞留時間 | 通常の2倍 |
| 再実行回数 | 2回超 |
| Blocked率 | 20%超 |
| 人間による差戻し率 | 15%超 |
| 根拠なし記述率 | 1件以上 |
| 同一ファイル更新回数 | 3回超 |
| 承認待ち時間 | 2営業日超 |
| 1タスクのエージェント数 | 6体超 |
| 入出力トークン比率 | 過去平均の2倍 |
| 成果物未参照率 | 作られたが使われていない |

---

## 9. 18役割カタログ（将来の責任分離候補）

18役割は「最初から作るAI社員」**ではなく**「将来、責任を分離するためのカタログ」として `05_Agents/_catalog.md` に保存する。

進化経路：

```
スキルとして運用
   ↓ 利用量・リスクが増加
独立エージェントへ昇格
```

昇格の判断は実測に基づく。例：

| 実測された問題 | 昇格させる役割 |
|----------------|----------------|
| 複数タスクの依存関係が崩れる | Orchestrator |
| コードレビュー精度が低い | QA Engineer |
| セキュリティ見落とし | Security Reviewer |
| 文体が安定しない | Content Editor |
| 業務ルール違反が発生 | Governance Auditor（常設化） |
| UI改善の品質が不足 | UI/UX Designer |

---

## 10. フェーズロードマップ

- **フェーズ1：最小ループ** — Knowledge Doer / Build Doer / Checker ＋ Planner補助 ＋ Auditor定義。Obsidian＋Git。タスクMarkdown＋JSON Linesログ。対象は個人用タスク管理アプリの仕様整理・UI改善案。夜間実行・自動修復はしない。
- **フェーズ2：スキルとタスク画面** — スキル3〜6個整備、SQLiteで実行状態管理、朝のブリーフ画面、承認キュー、実行/停止ボタン、コスト記録。
- **フェーズ3：低リスク自動化** — タスク整理、リンク検査、テスト実行、Markdown整形、ブリーフ生成。Routines/スケジューラ導入。
- **フェーズ4：必要な役割だけ分離** — §9の実測トリガーが出た領域だけ独立エージェントへ昇格。

---

## 11. フェーズ1で作るもの（Claude Code への具体的指示）

以下を作成すること。

1. **ディレクトリ構造**（§5）を作成。各フォルダに用途を書いた `README.md` を置く。
2. **`07_Policies/`** に以下を作成：
   - `constitution.md`（4制約＋7原則の要約）
   - `priority-order.md`（§4.3の優先順位）
   - `prohibited-actions.md`（共通禁止事項）
3. **`05_Agents/`** に §7 の定義を作成：`knowledge-doer.md` / `build-doer.md` / `checker.md` / `auditor.md` / `planner-prompt.md`、および `_catalog.md`（§9の18役割カタログ）。
4. **`06_Skills/`** に §3.3 の7スキルのスケルトン（各ディレクトリに `SKILL.md` の雛形）を作成。
5. **`02_Tasks/`** に §6 のスキーマに沿った `TASK-template.md` と、動作確認用のサンプルタスク1件を作成。
6. **`CLAUDE.md`** を生成し、§4の不変条件（特に4.1の単一書き手）と§4.3の優先順位、ティア別フロー（§3.4）を要約して載せる。これによりこれらが常時文脈に入る。
7. **`.gitignore`** を作成（端末固有の `.obsidian/` 設定、`runtime.db`、一時ファイル等を除外）。
8. 最後に **`README.md`**（ルート）で全体構造と「フェーズ1の回し方」を1ページに要約。

Routines を使う場合も、最初は次の1本のみ（§4.1の追記専用ルールを厳守）：

> リポジトリ内の `status: checking` のタスクを受入条件に照合し、結果を `10_Runs/` に新規レポートとして出力する。正式成果物・保護ブランチ・タスクの status は変更しない。

---

## 12. まだ作らないもの（明示指示があるまで禁止）

- SQLite / runtime.db（フェーズ2）
- 自作管理画面・ダッシュボード（フェーズ2）
- 夜間デイリーラン・自動修復（フェーズ3）
- Operations Doer / Orchestrator / 専門エージェント各種（フェーズ4・実測トリガー後）
- T2タスクのRoutinesによるかRoutines自律実行（恒久的に禁止）
- 8個目以降のスキル（必要が実証されてから）

---

## 13. 確定の経緯（参考）

この設計は次の収束を経ている：18役割型（将来カタログへ）→ Doer/Checker最小シェル型（基本骨格）→ 4つの修正（①Git＋実行状態の分離 ②CheckerとAuditorの概念分離 ③Doerを権限境界で3分割 ④Planner早期化）→ Routines×ローカルSQLite不整合の解消（ローカル系とクラウド系の物理分離）→ 可変状態の単一書き手という不変条件の確定。

**設計上の争点は解消済み。本仕様に従って実装してよい。**

# CLAUDE.md — 常時文脈に載せる不変条件

> このファイルは Claude Code が常に参照する。SPEC.md の不変条件（§4）・優先順位（§4.3）・ティア別フロー（§3.4）を要約している。
> 詳細は `SPEC.md` と `07_Policies/` を正とする。矛盾したら上位（`07_Policies/priority-order.md`）が勝つ。

---

## 0. 目的（判断の基準）

4制約を同時に満たしながら知的労働の成果量を増やす：**①品質を落とさない ②コストを抑える ③人間の関与を1日20〜30分以内 ④信頼性・監査性を保つ**。
判断に迷ったら常に「**人間の手間が増えないか**」で決める。

---

## 1. 最重要の不変条件：status の書き手は1つだけ（SPEC §4.1）

タスクの `status`（`02_Tasks/*.md` の frontmatter）を書き換えられるのは **ローカルの人間 / Claude Code だけ**。

- **Doer** は `doing → checking` まで。
- **Checker** は `checking → approval` のみ。
- **人間** のみ `approval → done`。
- **クラウド Routines は status を一切書き換えない（追記専用）**。レポートは `10_Runs/` に新規ファイルで出すだけ。

`status` の frontmatter 値が唯一の真実。進行中の状態を Git へ頻繁に書き戻さない（一段落したときだけ更新）。

---

## 2. 優先順位（矛盾時・低い階層は高い階層を上書きできない／SPEC §4.3）

```
1. 法令・セキュリティ規定
2. constitution（07_Policies/constitution.md）
3. プロジェクト要件（01_Projects/）
4. 個別タスク指示（02_Tasks/）
5. エージェント固有マニュアル（05_Agents/）
6. 過去事例・議事録
7. AI自身の推測
```

判断できない／上位同士が矛盾するときは、**勝手に進めず停止して人間に確認**（`blocked`）。

---

## 3. リスクティア別フロー（SPEC §3.4）

| ティア | 例 | 検査フロー |
|--------|-----|-----------|
| **T0** 下書き | アイデア、要約、UX改善案、調査メモ、比較案 | Doer → scratch出力 →（任意で人間が読む）。**Checker任意** |
| **T1** 隔離変更 | Markdown更新、開発ブランチ、テスト、リファクタ | Doer → **Checker必須** → 人間がマージ |
| **T2** 不可逆・外部 | 外部送信、本番反映、削除、顧客データ、権限変更、課金、セキュリティ設定 | Doer → Checker → **Auditor** → **人間承認** → 実行 |

- **T2 はクラウド Routines で自律実行させない**（承認プロンプトがないため。恒久ルール）。
- ティア判断に迷ったら安全側（上位）に倒す。
- **「迷う」の判定条件には、技術的な複雑さだけでなく「依頼者・承認者がその領域の専門知識を持たないと見受けられ、かつ領域にリスクがある（データ・本番・外部送信・権限に触れる）」場合も含める**（畑違い対応）。この場合、実際の技術的難易度に関わらず安全側（上位tier・Checker必須）に倒す。専門知識を持つ人へのレビュー依頼を勧めてよい。

### 3.1 思考ゲート（ティア連動・ADR-0006 提案1）

各タスクで通す思考ゲートを**ティアで濃さを変える**（一律に課さない＝原則6・制約3）。既存の部品に対応しており、新スキル・新人格は増やさない。

| ティア | スコープ | 証拠 | 対立的推論(プレモータム) | 検証 | 校正 |
|--------|:---:|:---:|:---:|:---:|:---:|
| **T0** | ✔ | ✔ | — | 任意 | — |
| **T1** | ✔ | ✔ | **✔** | ✔(Checker必須) | 任意 |
| **T2** | ✔ | ✔ | **✔** | ✔(Checker必須) | **✔**(1回) |

- スコープ＝タスクの objective/受入条件/禁止事項。証拠＝`source-verification`。検証＝Checker（3値判定）＋`qa-testing`。
- **対立的推論（プレモータム）**：T1/T2 は**着手前に1回**、「受入条件を満たせないとしたら原因は何か」を**3点**挙げて手当てする。方法は `06_Skills/task-decomposition`（パートB）。3点で十分・膨らませない。
- 校正＝T2 は着手成果を1回見直して最適化（既存の review/simplify 相当。専用スキルは作らない）。
- この濃度は SPEC §3.4 と一致させる。矛盾したら priority-order（constitution が上位）。

### 3.1.1 機械的セルフチェック条件（ティアと独立に適用）

Doerは、以下のいずれかに該当する数値・成果物について、完成前に**別の計算経路または別視点での再検証**を行う（「自分は気づけるはず」という主観判断に頼らない）：

1. 複数ステップの計算・変換を経た数値（単純な参照・コピーではないもの）
2. 人間が事後に元データと突き合わせずに信じる可能性が高い集計・合計・時間換算
3. 一度生成した後に人間が全ページ／全セルを目視しない分量の成果物（目安：3スライド超・50行超等）

該当しない場合（単純参照、その場で人間が全量を見る分量）は不要。ティア（T0/T1/T2）によるChecker要否の判定とは別に、Doer自身の作業品質として適用する（自分用の成果物・スクリプトも対象外にしない）。

### 3.2 確認・承認文言は「具体的な結果」を問う（知ったかぶりリスク対策）

依頼者・承認者がその領域の専門知識を持たない場合、技術的な説明を理解していなくても、それを言い出しにくく「分かったふり」でYesと答えてしまうことがある。**本人の不注意ではなく説明が伝わっていないことが原因**であり、自己申告（質問・確認してくれること）には頼れない。

- 確認・承認を求める文言は、常に**具体的な現実の結果**（例：「既存の〇〇データは元に戻せなくなります」「〇〇の内容が外部に公開されます」）を平易な言葉で問う。専門用語（tier、migration、downtime 等）は確認文言の表面に出さない（技術詳細は記録用に別途残してよい）。
- 抽象的な「これでよいですか」だけの確認は避ける。理解していなくても機械的にYesと言えてしまうため。
- 自己チェック基準：その確認文言は、その領域の専門知識が無い人でも「自分が気にする現実的な結果」として答えられる形になっているか。専門用語を言い換えずに使っていないか。

### 3.3 なりすまし・金銭アクセス・認証突破につながる情報、または特別法の保護対象は、T2着手前に法務判断を挟む

**前提**：この種のデータは、そもそもAI（Claude）が読める場所に置かないことが第一の防御線であり、これはあくまでそれが破られてしまった場合の安全装置（次善の備え）である。この確認自体、対象データの一部（列名等）に既に触れた上で行われる場合があり、「一切触れさせない」ことまでは保証できない——深い処理・出力生成をそれ以上進めないための備えである。

対象データが、①**実在する個人へのなりすまし・金銭への直接アクセス・認証突破**を可能にする情報（例：ログイン認証情報、口座・カード番号）、または②**個人情報保護法・不正競争防止法等、通常のT2判断より重い個別の法律で厳格に保護されている**情報（例：マイナンバー等の公的本人確認番号、通信の秘密に関わる識別子、信用情報、外部に知られると経営上重大な不利益が生じる非公開の営業秘密（原価・取引条件等））に該当する場合、通常のT2手続き（Doer→Checker→Auditor→人間承認）に入る前に、まずその可否自体を法務・情報システム部門など**権限を持つ人間**に確認する。**該当するか判断がつかない場合は、該当するものとして扱う**（安全側デフォルト）。氏名・住所・連絡先・生年月日等の通常の個人情報は、この事前確認の対象ではなく、従来どおりT2の通常フロー（Checker/Auditorを経て人間承認）で扱う。CheckerやAuditorは①②の可否判断の代替にならない。可否が確認できるまでは着手せず`blocked`とし、メモに理由（機密データ・法務確認待ち）を明記する。

---

## 4. 実行主体（3基本＋2条件付き／SPEC §3.1）

- **Knowledge Doer**（読み取り中心）：Bash・コード変更・外部送信・削除は禁止。
- **Build Doer**（書き込み・実行）：開発ブランチ限定。本番反映・削除・認証情報変更は禁止。
- **Checker**（全タスクの検査、T0は任意）：成果物 vs 受入条件を3値判定（適合/不適合/判定不能）。
- **Planner**（条件付き・補助）：依頼→タスク下書き。人間が確認してから実行。
- **Auditor**（条件付き・T2のみ）：ログ・承認記録 vs 工程・権限・規定・コスト上限。

Checker と Auditor は**異なる素材を異なる角度で**見る（相関故障を避ける／SPEC §4.2）。

---

## 5. ストア分離（SPEC §3.6）

- **Git（＝Obsidian Vault）**：確定記録（指示書・タスク・成果物・承認記録・確定ログ）。
- 実行中の可変状態は **タスク Markdown ＋ `10_Runs/` の JSON Lines ログ**で扱う。
- **フェーズ1では SQLite / runtime.db を作らない**（フェーズ2要素）。

---

## 6. やらないこと（SPEC §12・フェーズ1では禁止）

- SQLite / runtime.db、自作管理画面・ダッシュボード
- 夜間デイリーラン・自動修復
- Operations Doer / Orchestrator / 専門エージェント各種
- T2 タスクの Routines 自律実行（**恒久禁止**）
- 8個目以降のスキル（必要が実証されるまで）

仕様と異なる判断が必要になったら、勝手に進めず**人間に確認**する。

---

## 7. 自律の範囲（ガードレール／SPEC §3.4・ADR-0002）

逐次の「進めていいか」で人間を拘束しない。**下記の範囲は確認を取らず自動で進め、結果を `approval` キューに積む**。人間は都合のよいときにまとめて承認/差戻し。

**確認なしで進める（可逆・ローカル・T0/T1）**：タスク起票/下書き、調査・要件整理・分析・文書、ローカル/開発ブランチのファイル変更・ツール実装・テスト・リファクタ、Doer→Checker ループ実行と `doing→checking→approval` の遷移、`10_Runs` ログ、使用済みスキルの充実、`PROGRESS.md` 更新、ローカルコミット。小さな分岐は安全側の既定値で進め、根拠を成果物/ログに明記。

**必ず止めて確認（T2・不可逆・スコープ拡大）**：外部送信・本番反映・削除・顧客データ・権限変更・課金・セキュリティ設定（T2）／§12 ゲートの開放（SQLite・自作画面等）／保護ブランチへの push・リモート公開・PR／方針・優先順位・禁止事項の変更／要件が本質的に不明で成果物の方向が大きく変わる場合（このときだけ、質問は1回にまとめる）。

**作法**：逐次確認しない。まとめて実行→人間は approval キューで承認/差戻し。

---

## 8. 可逆性のガードレール（ADR-0003）

自動で進める前提として、**壊しても戻せる**ことを保証する。

**判定に依存しない（default-safe）**：「破壊的操作かどうか」を都度判定して退避を決めると、判定ミスで静かにすり抜ける。だから判定しない。**すべての変更を1経路に通し、その経路が無条件に変更前を退避する**。分類を間違えても復元点は必ず残る。

- **リポジトリ内**：復元点は Git。個別に「破壊的か」を判定せず、**着手時と各まとまりでコミット**する（常に直近の復元点がある）。既存ファイルの大きな上書きは旧版が Git にあることを確認（無ければ先にコミット）。`10_Runs/` は追記専用。
- **リポジトリ外データ（アプリの localStorage 等）**：全変更を単一の `mutate()` 等に通し、**毎回**履歴へ退避（多段Undo）。ボタンごとに退避要否を判断しない。最も重い操作（全消去）はバックアップJSONも書き出す。
- **ユーザーデータを復元不能な形で消さない。** 迷ったら「先に退避してから実行」。

---

## 9. セッション中断・再開の耐性（ADR-0004）

利用制限（5時間ごと等）やコンテキストクリアで、作業途中にセッションが打ち切られることは**前提として起きるもの**として扱う。会話の記憶ではなく、ファイル（タスクのメモ・`10_Runs/`・`PROGRESS.md`）だけで「どこまで進んだか」を再構成できる状態を常に保つ。

- **長時間・多段の作業**に着手する前に、タスクのメモへ簡単な**ステップリスト**を書き、各ステップ完了ごとにチェックを更新する。
- コミットは長時間タスクでは**より細かい単位**で行う（ADR-0003の粒度を強化）。1つの大きな成果物が完成するまで無コミットで抱え込まない。
- **新しいセッションでは、新しい依頼に着手する前に** `PROGRESS.md` の現在地と `status: doing/checking` のタスクの有無を確認し、あれば直近のメモ・ログから再開する（人間の別指示があればそちらを優先）。
- 人間側は中断に気づいても特別な操作は不要。次のセッションで話しかければ自動的にこの確認から始まる。

# ONBOARDING.md — AI Workspace 引き継ぎ手順書

> 対象読者：この環境について**前提知識ゼロ**の人。ただし手順を論理的に追える人。
> 所要時間：セットアップ10〜15分、全体理解30分程度。

---

## 0. これは何か（30秒で）

AIに調査・実装・文書作成などの知的作業を任せ、人間は**方針決定と最終承認だけ**を行う仕組みです。指示書・成果物・記録はすべてファイル（Git）で管理し、後から誰でも「なぜこうなったか」を追えます。クラウドは使わず、すべてローカルで完結します。

正典は [SPEC.md](SPEC.md)、常時参照される不変条件は [CLAUDE.md](CLAUDE.md)、進捗の正本は [PROGRESS.md](PROGRESS.md) です。この3つを読めば全体像が分かるように作ってあります。

---

## 1. 受け取るもの（必要ファイル一式）

**このフォルダ全体（Gitリポジトリ）をそのままコピー、または `git clone` してください。** 一部だけ渡すと機能しません（不変条件・過去の判断根拠・進捗がすべてファイルに分散しているため）。

### 含まれる主なもの
| フォルダ/ファイル | 内容 |
|---|---|
| `CLAUDE.md` / `SPEC.md` / `PROGRESS.md` | 不変条件 / 設計仕様 / 進捗の正本（最初に読む3点） |
| `07_Policies/` | 共通規定（憲法・優先順位・禁止事項） |
| `05_Agents/` | AIの役割定義（調査役・実装役・検査役・監査役） |
| `06_Skills/` | 専門スキル（実際に使われたものだけ中身が充実） |
| `02_Tasks/` | タスクの正本（`status` frontmatterが唯一の真実） |
| `08_Decisions/` | 重要な意思決定の記録（ADR） |
| `10_Runs/` | 実行ログ（追記専用） |
| `.git/` | **絶対に欠かさないこと**。監査証跡そのもの |

### 引き継ぎ不要（各自の環境で自動生成される・.gitignore対象）
- `.claude/settings.local.json`：あなたの権限許可リスト。前任者のものをコピーしない（パスがずれる）。
- `.obsidian/workspace*`：Obsidianの個人ウィンドウ配置。
- `runtime.db` 等：フェーズ1では存在しない想定。

### 要注意：機密データは別送が原則
プロジェクトによっては、機密データ（社内の実データ等）を `03_Outputs/` にメタ情報だけ残し、実データ本体はリポジトリに含めない「データ分離」運用を取ることがあります（意図的な設計）。そのようなタスクを引き継ぐ場合は、該当タスクファイル（`02_Tasks/TASK-*.md`）の「背景」や「詳細」に元データの所在が書いてあるので、そこを見て別途受け渡してください。

---

## 2. 事前に用意するもの（インストール）

1. **Claude Code**（CLIまたはデスクトップアプリ）と有効なアカウント。
2. **Git**（履歴を見る・コミットするために必須）。
3. **Python 3.x**（データ分析系タスクを行う場合。pandas / matplotlib / openpyxl / python-pptx 等は初回に導入が必要になることがあります）。
4. （任意）**Obsidian**（無料）— 人間がタスク状況を見やすくするためのビューア。無くても環境自体は動きます。
5. （任意）**Microsoft PowerPoint**（経営報告などPPTX成果物のレイアウト確認をする場合）。

---

## 3. 受け取った直後にやること（最初の10分）

1. フォルダを好きな場所に置く（例：`C:\Users\<あなたの名前>\Desktop\code\0200_ai-workspace`）。
2. そのフォルダを作業ディレクトリにして **Claude Code を起動**する。
3. 何も前提を与えず、まずこう聞いてください：
   > 「CLAUDE.md と PROGRESS.md を読んで、現状と次にやることを教えて」
   `CLAUDE.md` は自動でコンテキストに読み込まれるので、AIはこの1問でほぼ状況を説明できます。
4. （任意）Obsidian で同じフォルダを「保管庫（Vault）」として開く。「フォルダをvaultとして開く」を選び、このフォルダを指定するだけです。プラグイン「Dataview」を入れると、[HOME.md](HOME.md) が承認待ち・進行中・blockedを自動一覧表示します（手順は 08_Decisions や過去のやり取り、または今困ったら Claude に「Obsidianの設定を手伝って」と聞けば案内してくれます）。

---

## 4. 知っておくべき5つのルール（絶対に守ること）

1. **タスクの `status` を動かせるのは人間かClaude Codeだけ。** クラウド機能でこれを書き換えることはありません。
2. **リスクは3段階（T0/T1/T2）。** T2（外部送信・本番反映・削除・課金・権限変更・セキュリティ設定）は**必ず人間が最終承認**してから実行されます。
3. **通常の作業は確認なしで自動的に進みます。** AIが逐次「進めていいですか」と聞いてくることはありません。止まるのはT2の時、新しいレイヤーに着手する時（例：データベース導入）、方針が変わる時、要件が本質的に不明な時だけです（[ADR-0002](08_Decisions/ADR-0002-autonomy-guardrail.md)）。
4. **データを壊す操作の前には、必ず戻せる状態を作ってから実行します。** Gitコミットや自動バックアップがそれに当たります（[ADR-0003](08_Decisions/ADR-0003-reversibility-backup.md)）。
5. **クラウドは使いません。** 全部ローカルとGitで完結する方針です（[ADR-0001](08_Decisions/ADR-0001-no-cloud-local-only.md)）。定期実行やクラウド連携を提案されても、この方針を理由に断って構いません。

---

## 5. 日常の運用（あなたがやること）

- 定期的に `02_Tasks/` の `status: approval` のタスクを確認する（Obsidianを使っていれば HOME.md 上で自動一覧）。
- 内容を見て問題なければ、そのタスクファイルの frontmatter の `status` を **`done`** に書き換える。
- 問題があれば、理由をメモ欄に書いて `status` を **`doing`** に戻す（差し戻し）。
- 新しい依頼はそのままAIに話しかけるだけで構いません。AIが必要に応じてタスク化・実行・検査まで自動で進めます。

これ以外は基本的に何もしなくて大丈夫です。人間の関与を1日20〜30分に収めることがこの仕組みの目的だからです。

---

## 6. 困ったときは

| 困りごと | 見る場所 |
|---|---|
| 用語がわからない | [07_Policies/constitution.md](07_Policies/constitution.md)、[CLAUDE.md](CLAUDE.md) |
| なぜこの仕様・方針になったか知りたい | [08_Decisions/](08_Decisions/)（ADR一覧） |
| 今どこまで進んでいるか知りたい | [PROGRESS.md](PROGRESS.md)（正本）／[HOME.md](HOME.md)（Obsidian視点） |
| 権限確認ダイアログが頻発する | 各自の `.claude/settings.local.json` の話。AIに「権限の許可リストを整理して」と頼めば対応してくれる |
| AIの挙動がおかしい／想定と違う | 遠慮なく指摘してよい。指摘内容は次回以降の判断に反映される仕組みになっている |

---

## 7. 引き渡し完了チェックリスト

- [ ] フォルダを受け取り、`git log` で過去のコミット履歴が見える
- [ ] Claude Code がこのフォルダを作業ディレクトリとして起動できる
- [ ] `CLAUDE.md` と `PROGRESS.md` の内容を、自分の言葉で人に説明できる
- [ ] （任意）Obsidian でこのフォルダを開けている
- [ ] `status: approval` のタスクがどこにあるか分かる
- [ ] 5つのルール（§4）を読んだ

すべてチェックできれば引き継ぎ完了です。
